package org.example.props;

import org.junit.jupiter.api.BeforeAll;

import static org.junit.jupiter.api.Assertions.*;

class FlywayPropertyReaderTest {
    @BeforeAll
    void setup(){
        FlywayPropertyReader flywayPropertyReader = new FlywayPropertyReader();
    }

}